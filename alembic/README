
# TODO
services from goodtherapy should be parsed in the same way as modality
credentials finds modalities, add them to the user record

Update field overrides for those fields that shouldn't clobber if from NYDB